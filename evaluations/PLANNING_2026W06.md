# 📋 预测算法优化 Planning

**时间**: 2026-02-07  
**基于**: 本周回测 (2/3-2/6)  
**目标**: 将预测准确率从 16.7% 提升到 60%+

---

## 🔴 问题诊断

### 1. 核心问题：追涨杀跌

| 案例 | 预测 | 实际 | 问题 |
|------|------|------|------|
| 2/3 300751 | 强烈买入 @335 | 次日-1.9% | 乖离率3.5%已在高位，但仍发买入信号 |
| 2/3 002300 | 卖出 @8.69 | 次日+10%涨停 | 错过涨停行情 |
| 2/5 300751 | 卖出 @294 | 次日+2.7% | 恐慌性卖出信号，错过反弹 |
| 2/5 300666 | 减仓 @停牌 | 复牌+8.9% | 复牌逻辑错误 |

**根本原因**：
- LLM 对"强势"的判断倾向于线性外推
- 缺乏"反转预警"机制
- 情绪分析权重过高，被短期新闻带节奏

---

### 2. 时序错位问题

```
当前流程:
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ T日 15:00   │───▶│ T日 22:00   │───▶│ T+1日 9:30  │
│ 收盘数据    │    │ 生成报告    │    │ 执行交易    │
└─────────────┘    └─────────────┘    └─────────────┘
                         ▲
                         │ 问题：报告基于T日收盘价
                         │ T+1开盘可能已大幅变化
```

**影响**：
- 2/3报告推荐买入300751@335，2/4开盘337，追高入场
- 信号滞后导致"追涨"变成"接盘"

---

### 3. 预测逻辑缺陷

**当前权重分配**：
- 缠论分析: 60%
- 均线系统: 25%  
- 其他因素: 15%

**问题**：
1. 缠论信号识别可能不准确（依赖LLM解读K线描述）
2. 均线多头排列 ≠ 次日继续涨（常在高点形成多头）
3. 新闻情绪分析被"利好"误导（减持公告被解读为"利空出尽"）

---

### 4. 复牌股处理缺失

**300666 江丰电子案例**：
- 停牌前: 113.48
- 预测: 减仓（基于停牌前走势）
- 实际复牌: +8.9%

**问题**：没有复牌后的估值重估机制

---

## 🟢 优化方案

### 方案1: 增加反转预警层

```python
# 在 operation_advice 之前增加反转检测
def detect_reversal_risk(trend_data):
    """
    反转风险检测 - 高位预警
    """
    risks = []
    
    # 1. 连续上涨后追高风险
    if trend_data['consecutive_up_days'] >= 3:
        risks.append("连涨3日+, 追高风险")
    
    # 2. 乖离率预警 (更严格)
    if trend_data['bias_ma5'] > 3:  # 从5%降到3%
        risks.append(f"乖离率{trend_data['bias_ma5']:.1f}%, 回调风险")
    
    # 3. 涨停次日冷却
    if trend_data['prev_limit_up']:
        risks.append("涨停次日, 分歧风险")
    
    # 4. 量能背离
    if trend_data['price_up'] and trend_data['volume_down']:
        risks.append("量价背离, 后继乏力")
    
    return risks
```

**效果预估**: 可避免 2/3 追高买入 300751

---

### 方案2: 信号置信度衰减

```python
# 对信号强度进行时效衰减
def adjust_signal_confidence(signal, context):
    """
    信号置信度调整
    """
    confidence = 1.0
    
    # 连续同向信号衰减 (追涨杀跌惩罚)
    if context['prev_signal'] == signal:
        confidence *= 0.7  # 连续同向信号打7折
    
    # 极端行情后的信号衰减
    if abs(context['prev_change']) > 5:  # 前日涨跌幅>5%
        confidence *= 0.6  # 极端行情后信号打6折
    
    # 缠论信号与均线矛盾时衰减
    if context['chan_bullish'] != context['ma_bullish']:
        confidence *= 0.5  # 矛盾时打5折
    
    return confidence
```

**效果预估**: 减少 002300 的错误卖出信号

---

### 方案3: 复牌股特殊处理

```python
def handle_resume_trading(stock_info):
    """
    复牌股特殊处理
    """
    if stock_info['suspended']:
        return {
            'signal': '观望',
            'reason': '停牌中，复牌后观察',
            'confidence': 0
        }
    
    if stock_info['just_resumed']:  # 复牌首日
        # 检查重组类型
        if stock_info['resume_reason'] == '资产重组':
            return {
                'signal': '观望',  # 不给方向
                'reason': '复牌首日，等待价格发现',
                'confidence': 0.3
            }
    
    return None  # 正常处理
```

**效果预估**: 避免 300666 的错误减仓信号

---

### 方案4: 引入技术指标硬约束

```python
# 在 LLM 分析之前增加硬规则过滤
HARD_RULES = {
    'no_buy': [
        ('bias_ma5 > 5', '乖离率过高'),
        ('consecutive_up >= 5', '连涨5日'),
        ('rsi > 80', 'RSI超买'),
    ],
    'no_sell': [
        ('bias_ma5 < -5', '乖离率过低'),
        ('consecutive_down >= 5', '连跌5日'),
        ('rsi < 20', 'RSI超卖'),
    ]
}

def apply_hard_rules(llm_signal, indicators):
    """
    硬规则过滤 - 优先于LLM信号
    """
    if llm_signal in ['买入', '强烈买入']:
        for condition, reason in HARD_RULES['no_buy']:
            if eval(condition, indicators):
                return '观望', f'硬规则触发: {reason}'
    
    if llm_signal in ['卖出', '强烈卖出']:
        for condition, reason in HARD_RULES['no_sell']:
            if eval(condition, indicators):
                return '观望', f'硬规则触发: {reason}'
    
    return llm_signal, None
```

---

### 方案5: 历史准确率反馈

```python
# 记录每次预测及结果
class PredictionTracker:
    def __init__(self, db_path):
        self.db_path = db_path
    
    def log_prediction(self, date, code, signal, price, target, stop):
        """记录预测"""
        pass
    
    def log_result(self, date, code, actual_price):
        """记录实际结果"""
        pass
    
    def get_accuracy(self, code=None, days=30):
        """获取准确率"""
        pass
    
    def get_signal_performance(self, signal_type):
        """分信号类型准确率"""
        # 例: "强烈买入" 信号历史准确率
        pass
```

**用途**:
1. 追踪每种信号的历史表现
2. 动态调整信号权重
3. 识别系统性偏差

---

## 📊 优化优先级

| 优先级 | 方案 | 预期收益 | 实现难度 | 建议 |
|--------|------|----------|----------|------|
| P0 | 硬规则过滤 | 避免追高 | 低 | ✅ 立即实施 |
| P0 | 反转预警 | 降低亏损 | 低 | ✅ 立即实施 |
| P1 | 复牌股处理 | 避免误判 | 中 | 本周完成 |
| P1 | 信号置信度衰减 | 减少错误 | 中 | 本周完成 |
| P2 | 历史反馈系统 | 持续优化 | 高 | 下周启动 |

---

## 🎯 验证计划

### 第一阶段: 2/7-2/9 (本周五-周日)
1. 实施 P0 优化
2. 用历史数据回测验证
3. 目标: 准确率提升到 40%+

### 第二阶段: 2/10-2/14 (下周)
1. 实施 P1 优化
2. 实盘验证一周
3. 继续模拟交易记录
4. 目标: 准确率提升到 50%+

### 第三阶段: 2/17+ 
1. 建立历史反馈系统
2. 迭代优化
3. 目标: 准确率稳定在 60%+

---

## 📝 下一步行动

1. **今天** - 修改 `analyzer.py`，增加硬规则过滤
2. **今天** - 增加反转预警逻辑
3. **明天** - 用本周数据回测验证
4. **下周一** - 实盘跑新版本

---

*Created: 2026-02-07 03:55 PST*
